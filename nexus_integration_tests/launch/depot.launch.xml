<?xml version='1.0' ?>

<launch>
  <arg name="headless" default="true"/>
  <arg name="use_simulator" default="true"/>
  <arg name="sim_update_rate" default='100'/>

  <!-- RMF Common launch -->
  <include file="$(find-pkg-share nexus_integration_tests)/launch/rmf/common.launch.xml">
    <arg name="use_sim_time" value="$(var use_simulator)"/>
    <arg name="config_file" value="$(find-pkg-share nexus_integration_tests)/config/rmf/depot.building.yaml"/>
  </include>

  <!-- DeliveryRobot fleet adapter -->
  <group>
    <include file="$(find-pkg-share rmf_demos_fleet_adapter)/launch/fleet_adapter.launch.xml">
      <arg name="use_sim_time" value="$(var use_simulator)"/>
      <arg name="nav_graph_file" value="$(find-pkg-share nexus_integration_tests)/config/rmf/nav_graphs/0.yaml" />
      <arg name="config_file" value="$(find-pkg-share nexus_integration_tests)/config/rmf/deliveryRobot_config.yaml"/>
    </include>
  </group>

  <node pkg="nexus_workcell_orchestrator" exec="nexus_workcell_orchestrator" name="rmf_nexus_transporter" output="both">
    <param name="capabilities" value="[nexus::capabilities::RMFRequestCapability]"/>
    <param name="bt_path" value="$(find-pkg-share nexus_integration_tests)/config/rmf_bts"/>
  </node>

  <!-- Simulator launch -->
  <include if="$(var use_simulator)" file="$(find-pkg-share nexus_integration_tests)/launch/rmf/simulation.launch.xml">
    <arg name="headless" value="$(var headless)" />
    <arg name="world_path" value="$(find-pkg-share nexus_integration_tests)/config/rmf/depot.world" />
  </include>

  <!-- Bringup from integration tests -->
  <!-- TODO(luca) should we move the integration launch here instead? -->
  <include file="$(find-pkg-share nexus_integration_tests)/launch/launch.py">
    <arg name="headless" value="$(var headless)" />
    <arg name="use_sim_time" value="$(var use_simulator)"/>
  </include>

</launch>
